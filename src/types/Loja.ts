export type UsuarioLojista = {
  nome: string;
  cargo: string;
  email: string;
  avatar: string;
};

export type Loja = {
  nome: string;
  cidade: string;
  plano: string;
  metaMensal: number;
};
